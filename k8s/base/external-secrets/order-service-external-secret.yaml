apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: order-service-secrets
  namespace: compose-portal
spec:
  refreshInterval: 1h    # Re-sync from SSM every hour (handles rotation)
  secretStoreRef:
    name: aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: order-service-secrets    # Name of K8s Secret to create/update
    creationPolicy: Owner          # ESO owns this Secret lifecycle
  data:
    - secretKey: DB_URL            # Key in K8s Secret
      remoteRef:
        key: /compose-portal/order-service/db/url    # SSM Parameter path
    - secretKey: DB_USERNAME
      remoteRef:
        key: /compose-portal/order-service/db/username
    - secretKey: DB_PASSWORD
      remoteRef:
        key: /compose-portal/order-service/db/password
    - secretKey: REDIS_HOST
      remoteRef:
        key: /compose-portal/common/redis-host
    - secretKey: REDIS_PORT
      remoteRef:
        key: /compose-portal/common/redis-port
    - secretKey: MONGODB_URI
      remoteRef:
        key: /compose-portal/order-service/mongodb/uri